/**
 * STUB: Document generation trigger — logs only.
 * Replace with real gRPC call to Document Service later.
 */
async function triggerDocumentGeneration(applicationId) {
  console.log(`[STUB] Would call Document Service GenerateDocument for application ${applicationId}`);
  return 'stubbed';
}

module.exports = { triggerDocumentGeneration };
